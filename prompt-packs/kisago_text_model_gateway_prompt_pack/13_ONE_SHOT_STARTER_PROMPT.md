# ONE-SHOT STARTER PROMPT

Use the prompt pack in this directory to implement the Kisago Text Model Gateway and economical story-generation/evaluation architecture.

Start with `00_MASTER_EXECUTION_PROMPT.md`.

Before editing code:
- inspect the existing repository,
- read the existing handoff/project-memory mechanism,
- inspect the current image-model adapter as a reference,
- map the Prompt Playground, Gemini text integration, and evaluation flow.

Treat the pack as a recommended target architecture, not a rigid specification. Reuse existing abstractions and adapt the plan to the actual codebase.

Carry the implementation end-to-end unless a materially destructive ambiguity requires clarification.

When complete, update the existing handoff (or create and link a feature-specific handoff if appropriate) and give me the implementation report requested in the master prompt.
